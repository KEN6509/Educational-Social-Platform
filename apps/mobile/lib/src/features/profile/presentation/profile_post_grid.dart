part of 'profile_page.dart';

// The stateful post-grid implementation remains behaviorally unchanged; the
// feature-local part is its presentation boundary for ProfilePage.
typedef _ProfilePostGrid = _ProfilePostGridLegacy;
typedef _ProfileGridError = _ProfileGridErrorLegacy;
